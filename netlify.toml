# Netlify configuration file
# Alternative to _headers file
# Note: CSP frame-ancestors takes precedence over X-Frame-Options

[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "frame-ancestors *"
